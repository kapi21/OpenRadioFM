<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="hcn.permission.FM_PLUG_SERVICE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    
    <!-- V16: Permisos QS6/NWD para intentar eludir el error de apertura del socket /dev/audio-dsp -->
    <uses-permission android:name="android.permission.ACCESS_FM_RADIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_ROUTING" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAPTURE_AUDIO_OUTPUT" />
    <queries>
        <!-- NWD Service -->
        <package android:name="com.nwd.radio.service" />
        <intent>
            <action android:name="com.nwd.radio.service.ACTION_RADIO_SERVICE" />
        </intent>

        <!-- HCN Service (Legacy) -->
        <package android:name="com.hcn.autoradio" />
        <intent>
            <action android:name="com.hcn.autoradio.FM_PLUG_SERVICE" />
        </intent>

        <!-- Jancar IVI (IRadio en com.jancar.services) -->
        <package android:name="com.jancar.services" />
        <intent>
            <action android:name="com.jancar.services.action.radio" />
        </intent>
    </queries>

    <application
        android:usesCleartextTraffic="true"
        android:requestLegacyExternalStorage="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OpenRadioFm">

        <!-- V16: Android Auto Metadata -->
        <meta-data android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc"/>

        <activity
            android:launchMode="singleTask"
            android:name=".ui.main.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:screenOrientation="landscape"
            android:configChanges="uiMode|orientation|screenSize|smallestScreenSize|screenLayout"
            android:theme="@style/Theme.OpenRadioFm">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- V16: Radio Media Service for Android Auto / MediaSession -->
        <service android:name=".service.RadioMediaService"
            android:exported="true"
            android:permission="android.permission.BIND_MEDIA_BROWSER_SERVICE"
            android:foregroundServiceType="mediaPlayback"
            tools:ignore="ExportedService">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <!-- Recepción OEM de botones de medios en arranque en frío -->
        <receiver
            android:name=".service.MediaButtonBootstrapReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".widget.OpenRadioFmWidgetProvider"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/openradiofm_widget_info" />
        </receiver>

        <receiver
            android:name=".widget.RadioWidgetActionReceiver"
            android:exported="false" />

        <!-- HiHack: muchas ROM apagan accesibilidad al reiniciar; recordatorio al abrir la app -->
        <receiver
            android:name=".receiver.HihackBootReminderReceiver"
            android:exported="true"
            android:directBootAware="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- K706/QuickFish: el widget del launcher manda broadcasts "customize/radio/*" a la radio OEM -->
        <receiver
            android:name=".receiver.OemRadioWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="/customize/radio/pre" />
                <action android:name="/customize/radio/next" />
                <action android:name="/customize/radio/seek_up" />
                <action android:name="/customize/radio/seek_down" />
                <action android:name="/customize/radio/close" />
            </intent-filter>
        </receiver>

        <!-- V16.2: Radio Hijacker Service (Accessibility) -->
        <service android:name=".services.FactoryRadioHijackerService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>


</manifest>